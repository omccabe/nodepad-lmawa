extends ../layout

block content
  h2 Register
    form(method='post', action='/users')
      include fields
      div
        input(type = 'submit', value = 'Register')
        span \ or
          a(href='/sessions/new') Log In